# turn off keystroke repeating
xset r off
# switch keyboard layout
setxkbmap -option 'grp:menu_toggle'
# setup function keys
sleep 20; xmodmap $HOME/.Xmodmap
# Startup
exec --no-startup-id ~/bin/wallpaper.sh
